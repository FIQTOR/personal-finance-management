import Loading from "./Loading"

const LoadingPage = () => {
    return (
        <div className="w-full min-h-screen flex justify-center items-center bg-bg text-text transition-colors duration-300">
            <Loading />
        </div>
    )
}

export default LoadingPage
